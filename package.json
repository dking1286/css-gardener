{
  "name": "root",
  "private": true,
  "scripts": {
    "check_common_dependencies": "lumo --auto-cache -c scripts scripts/css_gardener/scripts/check_common_dependencies.cljs",
    "remove_build_caches": "lumo --auto-cache -c scripts scripts/css_gardener/scripts/remove_build_caches.cljs",
    "get_packages": "lumo --auto-cache -c scripts scripts/css_gardener/scripts/get_packages.cljs",
    "lint": "lerna run lint",
    "release": "lerna run release",
    "compile-tests": "lerna run compile-tests",
    "run-tests": "lerna run run-tests",
    "test": "lerna run test"
  },
  "dependencies": {
    "fs-extra": "9.0.1"
  },
  "devDependencies": {
    "lerna": "3.20.2",
    "lumo-cljs": "1.10.1"
  }
}